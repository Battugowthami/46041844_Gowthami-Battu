const colors = ['red'];
colors.push('green');
//console.log(colors); 

colors.pop();
colors.pop();
console.log(colors); 

const person = { age: 20 };
//person.age = 30; 
console.log(person.age);


let scores = [75, 80, 95];
for (const score of scores) {
    console.log(score);
}